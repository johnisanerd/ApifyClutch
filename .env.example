# This Actor has NO upstream data vendor and therefore no API key.
# It fetches the site directly using a browser-grade TLS fingerprint, and any
# proxy credentials come from the Apify platform at run time.
#
# Nothing is required here. The file exists so the shared test harness, which
# sources .env when present, has a documented shape.
