[project]
name = "ApifyClutch"
version = "1.0.0"
description = "Apify Actor that collects structured records from a web data source."
requires-python = ">=3.13"
dependencies = [
    "apify>=3.4,<4",
    "curl-cffi>=0.7,<1",
    "python-dotenv>=1.0,<2",
]

[tool.uv]
package = false
